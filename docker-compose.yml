services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: cornerpocket
      POSTGRES_USER: cp
      POSTGRES_PASSWORD: changeme
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
